{
  "custom": {},
  "params": {
    "label": "",
    "icon": "",
    "path": ""
  },
  "propConfig": {
    "params.label": { "paramDirection": "input" },
    "params.icon":  { "paramDirection": "input" },
    "params.path":  { "paramDirection": "input" }
  },
  "props": {
    "defaultSize": { "width": 200, "height": 36 }
  },
  "root": {
    "type": "ia.container.flex",
    "meta": { "name": "root" },
    "events": {
      "dom": {
        "onClick": {
          "config": { "script": "\tlogger = system.util.getLogger('BlueRidge.NavItem')\n\tpath = self.view.params.path\n\tlogger.info('NavItem clicked: path=%s' % path)\n\tif path:\n\t\ttry:\n\t\t\tsystem.perspective.navigate(path)\n\t\t\tlogger.info('navigate succeeded for %s' % path)\n\t\texcept:\n\t\t\tlogger.exception('navigate failed for %s' % path)\n\telse:\n\t\tlogger.warn('NavItem clicked but path param is empty/null')" },
          "scope": "C",
          "type": "script"
        }
      }
    },
    "propConfig": {
      "props.style.classes": {
        "binding": {
          "type": "expr",
          "config": {
            "expression": "if({page.props.path} = {view.params.path}, 'nav-item nav-item-active', 'nav-item')"
          }
        }
      }
    },
    "props": {
      "direction": "row",
      "alignItems": "center"
    },
    "children": [
      {
        "type": "ia.display.icon",
        "meta": { "name": "Icon" },
        "position": { "basis": "auto" },
        "propConfig": {
          "props.path": {
            "binding": { "type": "property", "config": { "path": "view.params.icon" } }
          }
        },
        "props": { "style": { "classes": "nav-icon" } }
      },
      {
        "type": "ia.display.label",
        "meta": { "name": "Label" },
        "position": { "grow": 1 },
        "propConfig": {
          "props.text": {
            "binding": { "type": "property", "config": { "path": "view.params.label" } }
          }
        }
      }
    ]
  }
}

EXEC Tools.Tool_CorrectShotCount
    @Id                = :id,
    @ShotCount         = :shotCount,
    @ExpectedShotCount = :expectedShotCount,
    @Note              = :note,
    @AppUserId         = :appUserId
